{% set list_classes = [
  "cob-icon-list",
  color ? "cob-icon-list--" ~ color : "",
  size ? "cob-icon-list--size-" ~ size : "",
] %}

<ul class="{{ list_classes | join(' ') | trim }}">
  {% for item in items %}
  <li class="cob-icon-list__item">
    <div class="cob-icon-list__icon {% if item.icon.color %} text-{{ item.icon.color }}{% endif %}">
      <svg class="cob-icon{% if item.icon.utilities %} {{ item.icon.utilities }}{% endif %}" aria-hidden="true" role="img">
        <use href="{{ img_path }}/sprite.svg#{{ item.icon.name }}"></use>
      </svg>
    </div>
    <div class="cob-icon-list__content">
      {{ item.content }}
    </div>
  </li>
  {% endfor %}
</ul>
