<section class="cob-hero" aria-label="Introduction">
  <div class="grid-container">
    <div class="cob-hero__callout">
      <h1 class="cob-hero__heading">
        {%- if hero.callout -%}
          <span class="cob-hero__heading--alt">{{ hero.callout }}</span>
        {%- endif -%}
        {{ hero.title }}
      </h1>
      {%- if hero.paragraph -%}
        <p>{{ hero.paragraph }}</p>
      {%- endif -%}
      {%- if hero.button -%}
        <a class="cob-button" href="{{ hero.button.href }}">{{ hero.button.text }}</a>
      {%- endif %}
    </div>
  </div>
</section>
