{% set base_label = search_label|default("Search") %}
{% set id = id|default("search-field") %}
{% set section_label = section_label|default("Search component") %}

{% if not search_js %}
  <section aria-label="{{ section_label }}">
{% endif %}
  <form class="cob-search {{ modifier }} {% if search_js %}js-search-form{% endif %}" role="search">
    <label class="cob-sr-only" for="{{ id }}">
      {{ base_label }}
    </label>
    <input class="cob-input" id="{{ id }}" type="search" name="search">
    <button class="cob-button" type="submit">
      {% if modifier != 'cob-search--small' %}
        <span class="cob-search__submit-text">
          {{ button_text|default(base_label) }}
        </span>
      {% endif %}
      <img
          src="./img/cob-icons-bg/search--white.svg"
          class="cob-search__submit-icon"
          alt="{{ button_text|default(base_label) }}"
      >
    </button>
  </form>
{% if not search_js %}
  </section>
{% endif %}
